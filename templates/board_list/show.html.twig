{% extends 'base.html.twig' %}

{% block title %}BoardList{% endblock %}

{% block content %}
    <h1>BoardList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ board_list.name }}</td>
            </tr>
            <tr>
                <th>Tableau</th>
                <td><a href="{{ path('board_show', {id: board_list.board.id } ) }}">{{ board_list.board.name }}</a></td>
            </tr>
            <tr>
                <th>IdTrello</th>
                <td>{{ board_list.idTrello }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('board_list_index') }}">back to list</a>

    <a href="{{ path('board_list_edit', {'id': board_list.id}) }}">edit</a>

    {{ include('board_list/_delete_form.html.twig') }}
{% endblock %}
