{% extends 'base.html.twig' %}

{% block title %}Board index{% endblock %}

{% block content %}
    <h1>Board index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>IdTrello</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for board in boards %}
            <tr>
                <td>{{ board.id }}</td>
                <td>{{ board.name }}</td>
                <td>{{ board.idTrello }}</td>
                <td>
                    <a href="{{ path('board_show', {'id': board.id}) }}">show</a>
                    <a href="{{ path('board_edit', {'id': board.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('board_new') }}" class="btn btn-primary">Create new</a>
{% endblock %}
